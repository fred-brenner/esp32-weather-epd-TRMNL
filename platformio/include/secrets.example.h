/*
 * Copy this file to secrets.h and replace the placeholder values.
 * secrets.h is ignored by git and must never be committed.
 */

#ifndef WEATHER_SECRETS_H
#define WEATHER_SECRETS_H

#define WEATHER_WIFI_SSID     "replace-with-your-wifi-ssid"
#define WEATHER_WIFI_PASSWORD "replace-with-your-wifi-password"
#define WEATHER_OWM_API_KEY   "replace-with-your-openweathermap-api-key"

#endif // WEATHER_SECRETS_H
